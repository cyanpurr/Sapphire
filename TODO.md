# TODO:
## Sapphire
- Faster ZIP library for extraction?
## Settings
- Have the option to adjust the time for storage checking.
- Install apps manually from .ZIP files for development testing.
## Applications
- Have an index for scripts however I'm not sure of a way this can be possible yet.
- Have the option for basic word filter bypassing when you install apps. (Adds <wbr> between every few letters of every word [ex: mario => ma<wbr>ri<wbr>o])
- Have the option for aggressive word filter bypassing when you install apps. (Replaces strings entirely with different ones [ex: tetris => blocksFalling])
## Indexes
- Implement categories.
- Implement tags.
- Implement version.
- Create an Index generator in Python for idiots.
- Sort applications by Index.
- Make it remove duplicate apps, this is if the title and description are the exact same.
